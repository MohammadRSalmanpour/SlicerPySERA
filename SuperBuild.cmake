# SlicerPySera/SuperBuild.cmake
# Minimal placeholder for external dependencies if needed.
cmake_minimum_required(VERSION 3.16.3)
project(SlicerPySera_SuperBuild)

message(STATUS "SuperBuild placeholder. Add external dependency rules here if required.")
# Example: external pyradiomics install steps could be added here for offline builds.
